# Nota de estudio

Consumo de proveedor externo mediante RestTemplate, RestClient y OpenFeign.

Se conserva como práctica de aprendizaje. Los artefactos generados (`target/`, `.class`) no forman parte de esta versión.
