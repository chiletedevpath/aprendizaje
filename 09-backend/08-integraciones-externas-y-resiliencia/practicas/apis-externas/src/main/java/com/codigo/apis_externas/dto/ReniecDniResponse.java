package com.codigo.apis_externas.dto;

public record ReniecDniResponse(    String first_name,
        String first_last_name,
        String second_last_name,
        String full_name,
        String document_number) {
}
