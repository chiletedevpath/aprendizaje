# Práctica · Configuración por entorno

Propón configuración para `dev` y `test`. La contraseña debe proceder de una variable de entorno. En `dev` permite SQL visible solo si se activa explícitamente; en `test` utiliza una base aislada.
