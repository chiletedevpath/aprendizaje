# Práctica · Contrato de capacitaciones

Define método, ruta, body y códigos de estado para: listar cursos, consultar uno, crear, actualizar parcialmente y eliminar. Justifica al menos un `404`, un `409` y un `201`.
